Ch12-aop-homework：使用aop做方法的参数检查

要求：
1. 当addNumber方法的参数是不为null并且大于0时，才能执行addNumber()。
2. 如果任意一个参数是null，或者小于0，则调用addNumber方法返回结果是-1；反之，则计算总和。
3. 使用aop做addNumber()方法的参数检查。

思路：
使用环绕通知。可以在目标方法之前先执行，并且可以根据标记（布尔数）修改目标方法的执行结果。